name: Schedule Heartbeat

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes (UTC) just for testing
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Show time
        run: |
          echo "UTC now: $(date -u)"
